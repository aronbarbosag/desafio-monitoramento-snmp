services:
  app:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped
  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - app
    restart: unless-stopped

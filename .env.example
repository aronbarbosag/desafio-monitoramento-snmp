# Se não definida, o app usa SQLite local (DB_PATH) automaticamente.
DATABASE_URL=postgresql+psycopg://snmp_monitor:snmp_monitor@localhost:5432/snmp_monitor

# Usado apenas quando DATABASE_URL não está definida.
DB_PATH=database.db

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Banco de dados local (artefato de runtime)
*.db
.pytest_cache/
*.docx

# Segredos locais (connection string do Postgres, etc.)
.env
.claude/
CLAUDE.md
graphify-out/

# Checkpoint de sessão (contexto pra retomar entre sessões) — não sobe pro repo
backend/docs/CHECKPOINT.md

# Planos/specs de sessão (superpowers) — artefatos de trabalho locais, não sobem pro repo
docs/superpowers/
